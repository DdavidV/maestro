defmodule MiniBank do
  @moduledoc """
  MiniBank: a small in-memory banking system, and a fully worked example
  of Maestro's extension points (`Maestro.Client` via `wire`/`db_client`,
  `Maestro.Assert.Matcher` via `money_equals`, `Maestro.Generator`) plus
  Maestro's own built-in `http` client and `json_schema_match` matcher,
  all exercised against a checked-in Maestro workspace (suites, a
  scenario, datasets, a test plan).

  See this app's own README.md for how to run it.
  """
end
